"""OAuth / OIDC router for Accuro.

Stub — full implementation added in a later task.
"""
from app.core.oauth import store_auth_code, consume_auth_code

__all__ = ["store_auth_code", "consume_auth_code"]
